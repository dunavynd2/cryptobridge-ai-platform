OPENAI_API_KEY=your_key_here
# Or use local models like Phi-3
POSTGRES_URI=postgresql://user:pass@localhost/db

# PKI Management Microservice: "api_key:role,api_key:role" pairs (roles: admin, service)
PKI_API_KEYS=dev-admin-key:admin,dev-service-key:service
