# Project Instructions

## Setup
1. `git clone ...`
2. `pip install -r requirements.txt`
3. Copy `.env.example` to `.env` and fill keys.
4. `python orchestrator.py` or `streamlit run frontend/app.py`

## Extending Agents
See individual `agents/*/INSTRUCTIONS.md`.

## Development Workflow
Follow the autonomous loop in orchestrator.

For full vision, refer to original pasted text.